These are the DLLs deployed with the original Qt Creator 3.6.1 for Windows.
Currently we use them here verbatim as precompiled binaries.
Later versions of LeanCreator will compile them from source integrated with
the LeanCreator build.

